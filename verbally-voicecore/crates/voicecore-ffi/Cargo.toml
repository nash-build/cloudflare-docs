[package]
name = "voicecore-ffi"
description = "C ABI for the voicecore engine: a stable, no_mangle surface that native shells (Swift/Kotlin/C/C++) link against. UniFFI-ready."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
voicecore = { path = "../voicecore" }

[lib]
crate-type = ["rlib", "cdylib", "staticlib"]
